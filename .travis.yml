language: go

go:
	- 1.3
	- tip

script:
	- go get github.com/axw/gocov/gocov
	- go get github.com/mattn/goveralls
	- go test ./... -v -covermode=count -coverprofile=profile.cov
	- goveralls -coverprofile=provile.cov -service=travis-ci -repotoken $COVERALLS_TOKEN